'use strict';

describe('Service: boxers', function () {

  // load the service's module
  beforeEach(module('angularGeneratedApp'));

  // instantiate service
  var boxers;
  //beforeEach(inject(function (_boxers_) {
  //  boxers = _boxers_;
  //}));

  it('should do something', function () {
    //expect(!!boxers).toBe(true);
  });

});
